const form=document.getElementById("chat-form"),input=document.getElementById("message-input"),messages=document.getElementById("messages"),button=form.querySelector("button");
function addMessage(text,type){const d=document.createElement("div");d.className="message "+type;d.textContent=text;messages.appendChild(d);messages.scrollTop=messages.scrollHeight}
form.addEventListener("submit",async e=>{e.preventDefault();const message=input.value.trim();if(!message)return;addMessage(message,"user");input.value="";button.disabled=true;try{const r=await fetch("/ask",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message})});const d=await r.json();addMessage(d.reply||"No response received.","bot")}catch(e){addMessage("Unable to connect to the AI service. Please try again.","bot")}finally{button.disabled=false;input.focus()}});